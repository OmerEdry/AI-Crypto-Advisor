import { Card } from '../../components/ui/Card';

// Placeholder. The form arrives in Step 13; the route exists now so the guards in this step are
// verifiable end to end.
export default function LoginPage() {
  return (
    <main className="mx-auto grid min-h-screen max-w-md place-items-center px-6">
      <Card>
        <h1 className="text-xl font-semibold">Sign in</h1>
        <p className="mt-2 text-muted">The sign-in form arrives in the next step.</p>
      </Card>
    </main>
  );
}
