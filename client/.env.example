# Local development only. The SPA runs on :5173 and the API on :4000, so the base URL is
# absolute here. In production the Vercel rewrite serves both from one origin and the default
# `/api` is correct, so this variable is not set on the hosting dashboard.
VITE_API_BASE_URL=http://localhost:4000/api
